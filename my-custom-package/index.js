//const upperCase = require("upper-case").upperCase;

function greet(name) {
  console.log(`hello ${name} ,welocm to ooty`);
}

greet("ashiq");
module.exports = greet;

//global package are primarily utility and tools which is return in nodejs  which help us in development
//eth  nammalk emport cheyyand use aaka
